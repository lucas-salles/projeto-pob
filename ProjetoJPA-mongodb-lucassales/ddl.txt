CREATE TABLE PRODUTO (ID  SERIAL NOT NULL, NOME VARCHAR(255), PRECO FLOAT, VERSAO INTEGER, TIPO_ID INTEGER, PRIMARY KEY (ID))
CREATE INDEX INDEX_PRODUTO_nome ON PRODUTO (nome)
CREATE TABLE CLIENTE (ID  SERIAL NOT NULL, CPF VARCHAR(255), NOME VARCHAR(255), PRIMARY KEY (ID))
CREATE INDEX INDEX_CLIENTE_nome ON CLIENTE (nome)
CREATE TABLE AUDITCLIENTE (ID  SERIAL NOT NULL, CPF VARCHAR(255), MOMENTO TIMESTAMP, NOME VARCHAR(255), OPERACAO VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE TIPO (ID  SERIAL NOT NULL, NOME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE CONTA (ID  SERIAL NOT NULL, ATIVO BOOLEAN, DATA TIMESTAMP, TOTAL FLOAT, CLIENTE_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE PRODUTO_CONTA (produtos_ID INTEGER NOT NULL, contas_ID INTEGER NOT NULL, PRIMARY KEY (produtos_ID, contas_ID))
ALTER TABLE PRODUTO ADD CONSTRAINT FK_PRODUTO_TIPO_ID FOREIGN KEY (TIPO_ID) REFERENCES TIPO (ID)
ALTER TABLE CONTA ADD CONSTRAINT FK_CONTA_CLIENTE_ID FOREIGN KEY (CLIENTE_ID) REFERENCES CLIENTE (ID)
ALTER TABLE PRODUTO_CONTA ADD CONSTRAINT FK_PRODUTO_CONTA_contas_ID FOREIGN KEY (contas_ID) REFERENCES CONTA (ID)
ALTER TABLE PRODUTO_CONTA ADD CONSTRAINT FK_PRODUTO_CONTA_produtos_ID FOREIGN KEY (produtos_ID) REFERENCES PRODUTO (ID)
